# 数据对接情况

## 1. 架构概述

系统采用**适配器模式**实现数据层解耦，支持多数据源切换：

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│   Vue 组件      │ ──▶ │   dataService    │ ──▶ │   数据适配器     │
│  (KPI/列表等)   │     │   (统一接口)      │     │  (CSV/API/...)  │
└─────────────────┘     └──────────────────┘     └─────────────────┘
```

### 核心文件

| 文件 | 职责 |
|------|------|
| `src/services/dataService.js` | 数据服务统一接口，适配器管理 |
| `src/services/dataTypes.js` | 数据类型定义，空结构工厂函数 |
| `src/services/adapters/csvAdapter.js` | CSV 数据适配器实现 |
| `src/config/index.js` | 配置管理（区域、KPI、阈值） |

## 2. 数据源配置

### 当前数据源（本地 CSV）

| 文件 | 路径 | 用途 |
|------|------|------|
| 任务数据 | `test_data/template_test.csv` | 项目任务、进度、工期 |
| 人员数据 | `test_data/template_test_people.csv` | 干系人、区域映射 |
| 资金数据 | `test_data/template_test_fund.csv` | 预留，暂未启用 |

### 加载方式

```javascript
// Vite ?url 方式加载
import urumqiTaskCsvUrl from '../../../test_data/template_test.csv?url'
import urumqiPeopleCsvUrl from '../../../test_data/template_test_people.csv?url'
```

### 编码处理

CSV 解析支持多种编码自动检测：
- UTF-8
- GB18030
- GBK
- GB2312

通过表头关键字匹配选择最佳解码结果。

## 3. 数据服务接口

### 适配器管理

```javascript
import { setDataAdapter, clearDataAdapter } from './services/dataService'
import { csvAdapter } from './services/adapters/csvAdapter'

// 设置适配器
setDataAdapter(csvAdapter)

// 清除适配器
clearDataAdapter()
```

### 数据获取接口

| 接口 | 参数 | 返回类型 | 说明 |
|------|------|----------|------|
| `getProjects()` | - | `Project[]` | 项目列表 |
| `getKpis(projectId)` | 项目ID | `KpiData[]` | 项目 KPI 卡片数据 |
| `getTaskData(projectId, kpi)` | 项目ID, KPI名称 | `TaskData` | 任务图表数据 |
| `getPersonnelData(projectId)` | 项目ID | `PersonnelData` | 人员列表 |
| `getRiskProjects(region)` | 区域 | `RiskProject[]` | 风险项目列表 |
| `getCompanyInsights()` | - | `CompanyInsights` | 公司级统计 |
| `getRegionalData(region)` | 区域 | `RegionalData` | 区域级数据 |
| `getProjectSeries(projectId)` | 项目ID | `number[]` | 进度兑现指数序列 |
| `getFundData(projectId)` | 项目ID | `FundData` | 资金数据（暂返回空） |
| `getExpenditureData(projectId)` | 项目ID | `ExpenditureData` | 支出数据（暂返回空） |

### 错误处理

所有接口在适配器未配置或调用失败时，返回符合接口定义的空数据结构：

```javascript
// 示例：无适配器时 getKpis 返回
[
  { title: '资金到账率', value: '0%', delta: '0%', up: true },
  { title: '任务完成率', value: '0%', delta: '0%', up: true },
  // ...
]
```

## 4. 字段映射

### 任务表（template_test.csv）

| 业务字段 | CSV 列名 | 说明 |
|----------|----------|------|
| 项目标识 | `项目简称` / `项目名称` | 优先使用简称 |
| 任务名称 | `工作项名称` | - |
| 任务ID | `工作项ID` | - |
| 任务状态 | `工作项状态` | 已完成/逾期完成/进行中等 |
| 责任人 | `责任人` / `执行人` | - |
| 计划开始 | `计划开始时间` | 支持 YYYY-MM-DD 等格式 |
| 计划完成 | `计划完成时间` | - |
| 实际开始 | `实际开始时间` | - |
| 实际完成 | `实际完成时间` | - |
| 计划工期 | `计划工期` | 数值（天） |
| 实际工期 | `实际工期` | 数值（天） |

### 人员表（template_test_people.csv）

| 业务字段 | CSV 列名 | 说明 |
|----------|----------|------|
| 项目标识 | `项目简称` / `项目名称` | - |
| 人员姓名 | `干系人` | - |
| 角色 | `角色` | - |
| 区域 | `区域` / `地区` | 用于项目-区域映射 |

## 5. 指标计算口径

### 5.1 任务完成率

```
计划完成率 = 计划完成日期 ≤ 当前日期的任务数 / 有计划完成日期的任务总数
实际完成率 = 实际完成日期 ≤ 当前日期的任务数 / 有计划完成日期的任务总数
```

### 5.2 逾期任务率

```
逾期任务 = 计划完成日期 < 当前日期 且 (未完成 或 实际完成日期 > 当前日期)
逾期率 = 逾期任务数 / 有计划完成日期的任务总数
```

排除状态为"逾期完成"和"已完成"的任务。

### 5.3 开工准点率

```
分母 = 计划开始日期 ≤ 今天的任务数
分子 = 实际开始日期 ≤ 计划开始日期的任务数
开工准点率 = 分子 / 分母
```

### 5.4 完工准点率

```
分母 = 计划完成日期 ≤ 今天的任务数
分子 = 实际完成日期 ≤ 计划完成日期的任务数
完工准点率 = 分子 / 分母
```

### 5.5 平均工期比

```
范围 = 实际完成日期 ≤ 今天 且 计划工期/实际工期均有效的任务
平均工期比 = avg(实际工期 / 计划工期)
```

### 5.6 人员健康度

```
风险成员 = 挂名项目数 > 3 的人员
健康度 = (总人数 - 风险成员数) / 总人数 × 100%
```

阈值可在 `src/config/index.js` 中配置：

```javascript
thresholds: {
  personnelRisk: {
    projectCountLimit: 3,  // 挂名项目超过此数量判定为风险
    healthyScore: 80,
    warningScore: 60
  }
}
```

### 5.7 进度兑现指数

综合多维度指标的加权评分：

| 指标 | 权重 | 计算方式 |
|------|------|----------|
| 开工准点率 | 10% | 准点开工任务数 / 本期应开工任务数 |
| 完工准点率 | 15% | 准点完工任务数 / 本期应完成任务数 |
| 关键里程碑达成率 | 35% | 暂无数据，评分为 0 |
| 平均工期比 | 15% | min(1, 1/工期比) |
| 逾期恢复时长 | 10% | min(1, 阈值/恢复天数)，阈值=3天 |
| 逾期积压率 | 7.5% | 基于逾期增量计算 |
| 逾期解决率 | 7.5% | 本期解决逾期数 / 上期逾期总数 |

## 6. 配置管理

### 区域配置

```javascript
// src/config/index.js
regions: [
  { id: 'all', name: '全国', isDefault: true },
  { id: 'east', name: '华东' },
  { id: 'south', name: '华南' },
  // ...
]
```

### KPI 配置

```javascript
kpis: [
  {
    id: 'task_completion',
    title: '任务完成率',
    compareMode: 'plan',  // 'plan' | 'period' | null
    showDelta: true,
    viewComponent: 'TaskCompletionView'
  },
  // ...
]
```

### 阈值配置

```javascript
thresholds: {
  personnelRisk: {
    projectCountLimit: 3,
    healthyScore: 80,
    warningScore: 60
  },
  projectRisk: {
    highRiskOverdueRate: 0.3,
    mediumRiskOverdueRate: 0.15
  }
}
```

## 7. 扩展数据源

### 实现新适配器

```javascript
// src/services/adapters/apiAdapter.js
export const apiAdapter = {
  async getProjects() {
    const response = await fetch('/api/projects')
    return response.json()
  },
  
  async getKpis(projectId) {
    const response = await fetch(`/api/projects/${projectId}/kpis`)
    return response.json()
  },
  
  // 实现其他接口...
}
```

### 切换数据源

```javascript
import { setDataAdapter } from './services/dataService'
import { apiAdapter } from './services/adapters/apiAdapter'

setDataAdapter(apiAdapter)
```

## 8. 数据服务接口状态

### 8.1 dataService 导出接口一览

| 接口 | 状态 | CSV适配器实现 | 说明 |
|------|------|---------------|------|
| `getProjects()` | ✅ 完整 | ✅ 已实现 | 从任务表+人员表聚合项目列表 |
| `getKpis(projectId)` | ✅ 完整 | ✅ 已实现 | 返回5个KPI卡片数据 |
| `getTaskData(projectId, kpi)` | ✅ 完整 | ✅ 已实现 | 任务完成率/逾期率图表数据 |
| `getPersonnelData(projectId)` | ✅ 完整 | ✅ 已实现 | 人员列表及项目挂名统计 |
| `getProjectSeries(projectId)` | ✅ 完整 | ✅ 已实现 | 进度兑现指数时间序列 |
| `getRiskProjects(region)` | ✅ 完整 | ✅ 已实现 | 风险项目列表（逾期率>15%） |
| `getCompanyInsights()` | ⚠️ 部分 | ⚠️ 部分实现 | 公司级统计，trend数据为空 |
| `getRegionalData(region)` | ⚠️ 部分 | ⚠️ 部分实现 | 区域数据，resourceLoad为空 |
| `getFundData(projectId)` | ⏳ 预留 | 🔲 返回空结构 | 资金到账数据，待接入 |
| `getExpenditureData(projectId)` | ⏳ 预留 | 🔲 返回空结构 | 项目支出数据，待接入 |
| `getProjectUpdates()` | ⏳ 预留 | 🔲 返回空数组 | 项目动态/日报，待接入 |
| `getWorkOrders()` | ⏳ 预留 | 🔲 返回空数组 | 工单列表，待接入 |
| `getAttributionData(kpi)` | ⏳ 预留 | 🔲 返回占位数据 | AI归因分析，待接入 |

### 8.2 KPI 卡片数据支持状态

| KPI | 配置ID | 数据状态 | 说明 |
|-----|--------|----------|------|
| 资金到账率 | `fund_arrival` | ⏳ 预留 | 固定返回 0%，待资金数据接入 |
| 任务完成率 | `task_completion` | ✅ 完整 | 基于计划/实际完成时间计算 |
| 关键里程碑达成率 | `milestone` | ⏳ 预留 | 复用任务完成率视图，待里程碑数据 |
| 逾期任务数 | `overdue_task` | ✅ 完整 | 基于计划完成时间和状态计算 |
| 项目支出金额 | `project_expenditure` | ⏳ 预留 | 固定返回 ¥0，待支出数据接入 |
| 人员健康度 | `personnel_health` | ✅ 完整 | 基于人员表项目挂名数计算 |

### 8.3 扩展指标数据支持状态

| 指标 | 数据状态 | 说明 |
|------|----------|------|
| 开工准点率 | ✅ 完整 | 基于计划/实际开始时间 |
| 完工准点率 | ✅ 完整 | 基于计划/实际完成时间 |
| 平均工期比 | ✅ 完整 | 基于计划/实际工期字段 |
| 逾期趋势比 | ✅ 完整 | 当前逾期率/上周逾期率 |
| 进度兑现指数 | ⚠️ 部分 | 关键里程碑达成率(35%权重)暂为0 |

### 8.4 待接入数据源

| 数据类型 | 预期来源 | 用途 |
|----------|----------|------|
| 资金数据 | `template_test_fund.csv` 或 API | 资金到账率、款项节点、逾期款项 |
| 支出数据 | API | 项目支出金额、月度趋势、分类支出 |
| 里程碑数据 | API | 关键里程碑达成率 |
| 项目日报 | API | 项目动态、AI分析素材 |
| 工单数据 | API | 工单列表、处理状态 |

### 8.5 接口架构说明

```
组件层 (*.vue)
    │
    ▼
dataService.js (统一接口层)
    │  - setDataAdapter() 设置适配器
    │  - safeAdapterCall() 安全调用+错误处理
    │  - 13个数据获取接口
    │
    ▼
适配器层 (adapters/)
    │
    ├── csvAdapter.js (当前使用)
    │     - 从本地CSV加载数据
    │     - 实现全部13个接口
    │
    └── apiAdapter.js (待实现)
          - 从后端API获取数据
```

所有组件均通过 `dataService` 获取数据，未直接依赖 `csvAdapter`，切换数据源只需在 `main.js` 中更换适配器即可。
