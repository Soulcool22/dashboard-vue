 # 数据对接情况（当前实现）
 
 ## 1. 数据源（当前为本地测试数据）
 
 - **任务数据**：`test_data/template_test.csv`
   - **入口**：`src/services/dataService.js` 内 `getAllTasks()`
   - **用途**：项目列表、任务完成率/逾期任务率、开工准点率、完工准点率、平均工期比、地区在建项目/风险项目等
 
 - **人员数据**：`test_data/template_test_people.csv`
   - **入口**：`src/services/dataService.js` 内 `getUrumqiPeopleRows()`
   - **用途**：人员列表、人员健康度、地区维度映射（项目 -> 地区）
 
 > 说明：目前数据通过 Vite `?url` 方式加载：
 > - `../../test_data/template_test.csv?url`
 > - `../../test_data/template_test_people.csv?url`
 
 ## 2. CSV 编码与解析规则
 
 - **编码**：数据服务使用 `fetch(...).arrayBuffer()` + `TextDecoder` 尝试多种编码：
   - `utf-8`
   - `gb18030`
   - `gbk`
   - `gb2312`
   通过“表头关键字”命中来选择最合适的解码结果（例如任务表会检查 `计划完成时间`、`工作项名称` 等）。
 
 - **解析器**：`parseCsv(text)`
   - 支持引号包裹字段
   - 会清洗不可见字符（BOM/零宽空格/NBSP）并 `trim()`
 
 - **日期解析**：`parseDateYmd(s)`
   - 支持 `YYYY-MM-DD` / `YYYY/M/D` / `YYYY.M.D`，可带时间（会忽略具体时分秒）
   - 解析失败返回 `null`
 
 ## 3. 关键字段映射
 
 ### 3.1 任务表（template_test.csv）核心字段
 
 - **项目标识**：
   - `项目简称`（优先）或 `项目名称`
 - **任务标识**：
   - `工作项名称`
   - `工作项ID`
 - **状态与人员**：
   - `工作项状态`
   - `责任人` / `执行人`
 - **时间字段**：
   - `计划开始时间`
   - `计划完成时间`
   - `实际开始时间`
   - `实际完成时间`
 - **工期字段**：
   - `计划工期`
   - `实际工期`
 
 > 当前实现中，任务对象在 `getAllTasks()` 中被标准化为：
 > - `projectId` / `projectName`：来自 `getRowProjectLabel(row)`（`项目简称`/`项目名称`）
 > - `planStart` / `planEnd` / `actualStart` / `actualEnd`：由 `parseDateYmd` 解析得到
 
 ### 3.2 人员表（template_test_people.csv）核心字段
 
 - **项目标识**：`项目简称`（优先）或 `项目名称`
 - **人员**：`干系人`
 - **地区/区域**：`区域`（优先）或 `地区`
 
 ## 4. 项目识别与匹配机制
 
 - `getRowProjectLabel(row)`：统一从 `项目简称`/`项目名称` 取项目展示名。
 - `normalizeProjectKey(value)`：
   - 清洗不可见字符
   - `trim()`
   - `toLowerCase()`
 - `getProjects()`：从任务表 + 人员表汇总项目，形成项目列表。
 - `getTasksByProject(projectId)`：使用归一化 key 过滤任务，当前逻辑：
   - `normalize(task.projectId) === key` **或** `normalize(task.projectName) === key`
 
 ## 5. 地区维度（区域页/全部管理视点）
 
 - 项目 -> 地区映射来自人员表：`getProjectRegionMap()`
 - 地区字段来源：`getRowRegion(row)`，取 `区域`/`地区`
 - 使用场景：
   - `getRegionalData(region)`：地区页在建项目、重点项目监控、资源负载
   - `getRiskProjects(region)`：按地区筛选后输出 Top 风险项目
   - `getCompanyInsights()`：全国（全部）人员健康度统计
 
 ## 6. 主要数据接口（src/services/dataService.js 导出）
 
 - **`getProjects()`**
   - 返回项目列表，供左侧“关注项目/项目列表”展示。
   - 每个项目包含 `expandedMetrics`（开工/完工准点率、平均工期比等）。
 
 - **`getKpis(projectId)`**
   - 返回项目 KPI 卡片（进度、逾期等）。
 
 - **`getTaskData(projectId, kpi)`**
   - 返回图表序列数据（例如 `任务完成率`、`任务逾期率`）。
 
 - **`getPersonnelData(projectId)`**
   - 返回项目人员列表（同名人员会统计跨项目挂名数量 `projectCount`）。
 
 - **`getCompanyInsights()`**
   - 返回“全部（管理视点）”数据：人员健康度等。
   - **已清除/置空**：
     - `trend`（项目群交付趋势）返回空数组
     - `deliveryEfficiencyLabel` 返回空字符串（当前无项目级交付数据支撑）
 
 - **`getRegionalData(region)`**
   - 返回地区页：在建项目、当月计划/交付、重点项目监控、资源负载。
 
 - **`getRiskProjects(region)`**
   - 返回重点项目监控（Top 3 风险项目）。
 
 - **占位接口（当前返回空/0）**
   - `getProjectSeries(projectId)`
   - `getProjectUpdates()`
   - `getWorkOrders()`
   - `getAttributionData(kpi)`
   - `getFundData(projectId)` / `getExpenditureData(projectId)`
 
 ## 7. 已实现指标口径（关键）
 
 ### 7.1 任务完成率
 - **函数**：`buildCompletionSeries(tasks)`
 - **分母**：有 `计划完成时间(planEnd)` 的任务数
 - **计划完成率**：`planEnd <= 日期` 的比例
 - **实际完成率**：`actualEnd <= 日期` 的比例
 
 ### 7.2 任务逾期率（已按新规则实现）
 - **函数**：`buildOverdueSeries(tasks)`
 - **判定**：基于 `计划完成时间`，排除 `逾期完成/已完成`，超过计划且未完成（含未开始）计为逾期
 
 ### 7.3 开工准点率
 - **函数**：`buildOnTimeMetrics(tasks)` -> `computeStartRateWithTotal(asOf)`
 - **分母**：`计划开始时间(planStart) <= 今天` 的任务数
 - **分子**：`实际开始时间(actualStart) <= 计划开始时间(planStart)` 的任务数
 
 ### 7.4 完工准点率
 - **函数**：`buildOnTimeMetrics(tasks)` -> `computeCompleteRateWithTotal(asOf)`
 - **分母**：`计划完成时间(planEnd) <= 今天` 的任务数
 - **分子**：`实际完成时间(actualEnd) <= 计划完成时间(planEnd)` 的任务数
 
 > 注意：如果项目内所有纳入统计的任务都“实际完成晚于计划完成”，则完工准点率会为 0%。
 > 若期望“日期缺失用状态兜底”等口径，需要再明确需求后调整该逻辑。
 
 ### 7.5 平均任务工期比（平均工期比）
 - **函数**：`buildOnTimeMetrics(tasks)` -> `computeAvgDurationRatioWithTotal(asOf)`
 - **计算**：对 `actualEnd <= 今天` 且 `计划工期/实际工期` 均有效的任务，求 `avg(actualDuration / planDuration)`
 
 ### 7.6 人员健康度（全部管理视点/地区资源负载）
 - **函数**：`getCompanyInsights()` / `getRegionalData(region)`
 - **口径**：按 `干系人` 聚合其参与的项目数（同名跨项目），`projectCount > 3` 记为风险成员
 
 ## 8. 当前限制与说明
 
 - **项目群交付趋势/交付效率**：当前无“项目级交付”数据（不是任务级），已在接口层置空。
 - **完工准点率异常（白云项目）**：若任务数据中大量存在 `实际完成时间 > 计划完成时间`，按当前口径显示 0% 属正常结果；如需改变口径需单独确认。
